# aria_agent.py -- LiveKit Video Agent
# Python 3.12 | Pinned 2026-03-25
# Upgrade via staging branch + Dependabot PR only

# LiveKit agent framework
livekit-agents[openai,silero,simli,deepgram]==0.12.0

# Database
supabase==2.10.0

# HTTP client
httpx==0.27.0

# Environment
python-dotenv==1.0.0

# Google Drive integration
google-api-python-client==2.149.0
google-auth==2.35.0

# Dropbox integration
dropbox==12.0.2

# NOTE: FastAPI/uvicorn/websockets/twilio intentionally excluded --
# those live in the call_handler service only.
# railway_security.py is a local file -- no pip install needed.
