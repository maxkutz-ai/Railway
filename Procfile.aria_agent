# Procfile for aria_agent (LiveKit video service)
# Two processes: worker runs the LiveKit agent, web serves health + webhook endpoint
web:    uvicorn aria_webhook:app --host 0.0.0.0 --port ${PORT:-8080}
worker: python aria_agent.py start
